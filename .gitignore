# Variáveis de ambiente (API keys, tokens — NUNCA commitar)
.env
.env.local
.env.*

# Arquivo gerado pelo /setup com MCPs específicos do usuário
_configurar-mcps.md

# Outputs de renderização (binários pesados)
*.png

# Arquivos de dados do usuário (CSVs, planilhas, etc)
dados/*.csv
dados/*.xlsx
dados/*.xls
dados/*.txt
dados/*.pdf
dados/*.json
!dados/README.md

# Pastas de trabalho genéricas (do template original)
projetos/
briefings/
trabalho/
estudos/
publicacoes/
anotacoes/
curriculo/

# Sistema
.DS_Store
node_modules/
